baseURL = "https://username.github.io"
title = "My Blog"
languageCode = "en-us"
theme = "PaperMod"

# Pagination configuration
[pagination]
  pagerSize = 5

# Enable search
[outputs]
  home = ["HTML", "RSS", "JSON"]

# PaperMod features
[params]
  showReadingTime = true
  showShareButtons = true
